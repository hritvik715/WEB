//imp-> In TypeScript, annotation refers to specifying the type of a variable, function parameter, return value, or any other construct using TypeScript's type system. 

type Person56 = {
    name: string;
};

const new_person: Person56 = {
    name: "john",
};



console.log(new_person.name);